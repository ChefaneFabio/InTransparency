import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Explore Verified Talent | InTransparency',
  description: 'Browse verified student profiles, skills, and academic achievements. Find the right talent for your organization.',
}

export default function ExploreLayout({ children }: { children: React.ReactNode }) {
  return children
}
