import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Developers — API Documentation | InTransparency',
  description: 'Integrate verified student data, talent search, and placement tracking into your systems with the InTransparency API.',
}

export default function DevelopersLayout({ children }: { children: React.ReactNode }) {
  return children
}
