import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'For Companies — Hire Verified Graduates | InTransparency',
  description: 'Access a pool of verified graduates with transparent academic records. Reduce hiring risk with trusted credentials.',
}

export default function ForCompaniesLayout({ children }: { children: React.ReactNode }) {
  return children
}
